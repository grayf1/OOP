// Polymorphism is to overwrite methods to change their behavior.
public interface Major {
    // define two methods 
    public Integer creditsRemaining(Integer creditsRequired);
    public boolean isMajorComplete(Integer creditsRequired);
}
